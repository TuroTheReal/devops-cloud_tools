# Stack entry point. Add your resources or module calls here.
# Keep one stack focused on one coherent set of infra (one blast radius):
# e.g. a network foundation, OR a data layer, OR an app layer, not all at once.
#
# Example module call:
# module "network" {
#   source       = "../../modules/network"
#   project_name = var.project_name
#   environment  = var.environment
# }
